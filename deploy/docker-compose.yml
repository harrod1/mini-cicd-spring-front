services:
  backend:
    image: harrod1/mini-cicd-backend:local
    container_name: mini-cicd-backend
    ports:
      - "8080:8080"

  frontend:
    image: harrod1/mini-cicd-frontend:local
    container_name: mini-cicd-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
